package com.ogana.backoffice.repository;

import com.ogana.backoffice.domain.FaceDetailSection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FaceDetailSectionRepository extends JpaRepository<FaceDetailSection, String> {
}
